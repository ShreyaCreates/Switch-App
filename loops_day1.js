const availableSkills = ["Python", "Flutter", "Design", "React", "Java"]
const skillPrices = [10, 150, 50, 80, 30]
for (let i=0; i<availableSkills.length; i++){
    console.log(availableSkills[i], "costs", skillPrices[i], "points")
}